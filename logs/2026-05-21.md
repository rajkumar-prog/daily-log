# 2026-05-21

## What I worked on
- Fixed Retry-After header handling in wandb/weave Node SDK — 429 rate limit responses were being retried blindly without respecting the server's requested wait time
- Added `parseRetryAfterMs()` helper, added 429-specific logic with a 60s threshold
- Addressed Codex automated review: (1) ensured 429 logic respects `retryOnStatus`, (2) rejected negative Retry-After values
- Opened PR #6922 to wandb/weave
- Fixed `FactualCorrectness` recall bug in ragas — recall mode was using precision TP (`reference_response`) instead of recall TP (`response_reference`), returning inflated scores
- Updated `fbeta_score` signature to take `tp_precision` and `tp_recall` separately
- Added 9 unit tests including explicit regression check for the reported wrong value (0.75 → 0.667)
- Opened PR #2737 to ragas (explodinggradients/ragas, now at vibrantlabsai/ragas)

## Issues explored
- wandb/weave Node SDK — no Retry-After header support for 429 responses
- ragas #2693 — `FactualCorrectness(mode="recall")` returns precision score instead of recall

## Notes
- GitHub org `explodinggradients` was renamed to `vibrantlabsai` — same repo, different URL
- F1 needs two separate TPs: `tp_precision` for the precision leg, `tp_recall` for the recall leg — using one for both silently inflates scores
- PR #6922: https://github.com/wandb/weave/pull/6922
- PR #2737: https://github.com/vibrantlabsai/ragas/pull/2737
