# 2026-05-21

## What I worked on
- Added `Retry-After` header support in wandb/weave Node SDK — 429 responses were being retried immediately without respecting the server's wait time; added `parseRetryAfterMs()` to handle both seconds and date formats, with 60s threshold
- Fixed `FactualCorrectness` recall bug in ragas — recall and F1 modes were using `reference_response` (precision TP) instead of `response_reference` (recall TP), returning inflated scores; renamed variables and updated `fbeta_score` signature; added 9 unit tests

## PRs
- wandb/weave #6922 — https://github.com/wandb/weave/pull/6922
- ragas #2737 — https://github.com/vibrantlabsai/ragas/pull/2737
